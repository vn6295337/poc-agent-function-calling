# Core dependencies
python-dotenv==1.0.0

# LLM providers (install at least one)
openai>=1.12.0
anthropic>=0.18.0

# Optional: Streamlit for UI
streamlit>=1.31.0

# Optional: Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
